---
description: "Save current project state for next session"
---

Save current project state. Runs `/continue save`.

When the user says `/wrapup`, execute the `/continue save` flow from the continue command.
